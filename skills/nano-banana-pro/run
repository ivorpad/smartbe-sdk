#!/usr/bin/env bash
set -euo pipefail

# SmartBe skill wrapper — resolves secrets before running the skill script.
# Generated by SmartBe skill installer. Do not edit.

SKILL_DIR="$(cd "$(dirname "$0")" && pwd)"

# Resolve required secrets via control plane
export GEMINI_API_KEY="$(smartbe-secret GEMINI_API_KEY)"

exec uv run "${SKILL_DIR}/scripts/generate_image.py" "$@"
